# Evolution of Todo - Environment Variables Template
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to git (it's in .gitignore)

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Development: Use local PostgreSQL or Neon free tier
# Production: Use Neon or managed PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/todo_dev

# ============================================
# OPENAI CONFIGURATION (Phase III+)
# ============================================
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-api-key-here

# Model selection: gpt-4-turbo (recommended), gpt-4, gpt-3.5-turbo
OPENAI_MODEL=gpt-2.5 flash

# ============================================
# AUTHENTICATION
# ============================================
# Generate with: openssl rand -hex 32
# Minimum 32 characters required
BETTER_AUTH_SECRET=your-secret-minimum-32-characters-here

# URL where your frontend is hosted
# Development: http://localhost:3000
# Production: https://your-domain.com
BETTER_AUTH_URL=http://localhost:3000

# ============================================
# API CONFIGURATION
# ============================================
# Backend API URL (must be prefixed with NEXT_PUBLIC_ for client access)
# Development: http://localhost:8000
# Production: https://api.your-domain.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================
# ENVIRONMENT
# ============================================
# Options: development | staging | production
ENVIRONMENT=development

# Logging level: DEBUG | INFO | WARNING | ERROR
# Development: DEBUG
# Staging: INFO
# Production: WARNING
LOG_LEVEL=DEBUG

# ============================================
# PHASE V ONLY (Event-Driven Architecture)
# ============================================
# Kafka broker addresses
# Local: localhost:9092
# Kubernetes: kafka:9092
# Redpanda Cloud: your-cluster.redpanda.cloud:9092
KAFKA_BROKERS=localhost:9092

# Dapr HTTP port (default: 3500)
DAPR_HTTP_PORT=3500

# Redpanda Cloud URL (if using managed Kafka)
REDPANDA_CLOUD_URL=

# ============================================
# OPTIONAL CONFIGURATION
# ============================================
# JWT expiration in seconds (default: 604800 = 7 days)
JWT_EXPIRATION_SECONDS=604800

# Rate limiting (requests per minute per user)
RATE_LIMIT_PER_MINUTE=100

# Maximum tasks per user (soft limit with warning)
MAX_TASKS_PER_USER=10000

# Maximum conversation length (messages per conversation)
MAX_CONVERSATION_LENGTH=100

# ============================================
# DEVELOPMENT ONLY
# ============================================
# Enable detailed error messages (never in production)
# Options: true | false
MAX_CONVERSATION_LENGTH=100

# Database connection pool settings
# Development: smaller pool
# Production: larger pool (20-40)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# ============================================
# NOTES
# ============================================
# - Never commit .env.local to version control
# - Use different secrets for each environment
# - Generate secrets with: openssl rand -hex 32
# - See specs/technical/environment-config.md for details
